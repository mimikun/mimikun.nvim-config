---@type table
local color = {
  -- wezterm: Use tint to replace icon colors while preserving alpha:
  --tint = "#89b4fa",
  tint = nil,
  -- wezterm: Color transforms are applied while building the PNG cache:
  --saturation = -100,
  saturation = 0,
  brightness = 0,
  hue = 0,
  monochrome = false,
}

return color
