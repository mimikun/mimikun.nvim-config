---@type LazyKeysSpec[]
local keys = {
  {
    -- TODO: it
    "<lhs>",
    function()
      -- TODO: it
    end,
    mode = {
      "n",
      -- TODO: it
      --"x",
      --"v",
    },
    desc = "",
    -- TODO: it
    --expr = true,
    --noremap = true,
    silent = true,
  },
}

return keys
