---@type LazySpec
local spec = {
  "DrKJeff16/wezterm-types",
  --lazy = false,
  --version = false,
  event = require("plugins.wezterm-types.events"),
  --cond = false,
  --enabled = false,
}

return spec
