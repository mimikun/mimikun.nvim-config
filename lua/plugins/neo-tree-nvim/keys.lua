---@type LazyKeysSpec[]
local keys = {
  {
    -- TODO: it
    "<lhs>",
    function()
      -- TODO: it
    end,
    mode = "n",
    desc = "",
    --expr = true,
    --noremap = true,
    silent = true,
  },
}

return keys
