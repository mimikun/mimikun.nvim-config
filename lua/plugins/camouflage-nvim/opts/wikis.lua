local wikis = {
  style = "stars", -- 'stars' | 'dotted' | 'text' | 'scramble'
  auto_enable = true, -- Automatically mask on file open
  reveal = {
    follow_cursor = false, -- Set true to auto-reveal current line
  },
  yank = {
    confirm = true, -- Ask before copying to clipboard
  },
}
