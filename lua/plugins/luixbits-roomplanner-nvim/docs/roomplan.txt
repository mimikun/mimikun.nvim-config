*roomplan.txt*  Keyboard-first floor planning for Neovim

==============================================================================
CONTENTS                                                        *roomplan*

1. Introduction                                      |roomplan-introduction|
2. Installation                                             |roomplan-install|
3. First plan                                            |roomplan-quickstart|
4. Commands                                               |roomplan-commands|
5. Workspace                                             |roomplan-workspace|
6. Keymaps                                                 |roomplan-keymaps|
7. Configuration                                           |roomplan-config|
8. Windows and outlets                              |roomplan-wall-features|
9. Furniture catalogues                                 |roomplan-furniture|
10. Aspect and view rotation                              |roomplan-display|
11. Storage and safety                                    |roomplan-storage|
12. Lua API                                                   |roomplan-api|
13. Health and troubleshooting                            |roomplan-troubles|

==============================================================================
1. INTRODUCTION                                      *roomplan-introduction*

RoomPlan stores exact millimetre geometry in structured JSON or a marked
Neorg block and renders an interactive terminal view. Display rounding,
zooming, aspect calibration, and view rotation never alter saved geometry.

Schema v4 supports one floor, connected rectangular-union room and furniture
footprints, shared-wall doors/windows, typed wall/floor outlets, and furniture
rotations in 90-degree steps. Existing schema-v1/v2/v3 plans migrate in memory
and are rewritten only by an explicit save. RoomPlan is a space planner, not
CAD/BIM software or a building-code checker.

An offline sun study persists exact plan north, location, and UTC offset, then
draws transient window light patches for a chosen date and time.

Requirements:

- Neovim 0.10 or newer
- no mandatory runtime dependencies
- Neorg only when embedding plans in .norg files

The complete linked handbook is in docs/README.md in the repository.

==============================================================================
2. INSTALLATION                                             *roomplan-install*

The repository must be on 'runtimepath' before Lua calls require("roomplan").
The plugin loader registers commands automatically; setup() is optional when
the defaults are sufficient.

lazy.nvim: >lua
  {
    "LuixBits/luixbits-roomplanner.nvim",
    version = "*",
    lazy = false,
    main = "roomplan",
    opts = {},
  }
<

Neovim 0.12 vim.pack: >lua
  vim.pack.add({
    {
      src = "https://github.com/LuixBits/luixbits-roomplanner.nvim",
      version = vim.version.range("*"),
    },
  })
  require("roomplan").setup({})
<

These examples follow the newest tagged release. Remove version to follow the
development branch instead. For exact pins, Nix flakes, nvf, rocks.nvim,
native packages, and local checkout examples, see
docs/getting-started/installation.md.

RoomPlan uses vim.ui.input() and vim.ui.select() for transient prompts. Neovim
provides defaults; an optional provider such as Snacks can replace those
prompts without taking over RoomPlan's workspace. See
docs/configuration/ui-providers.md for setup examples.

==============================================================================
3. FIRST PLAN                                            *roomplan-quickstart*

Create a standalone plan without overwriting an existing non-empty file: >vim
  :RoomPlanInit ~/plans/flat.roomplan.json
<

Press `a` to add an object and use `h`/`j`/`k`/`l` to move the Canvas cursor.
Use `e` for exact properties and `m`/`r`/`R` to move, resize, or rotate the
selected object when supported. Press `s` to save, `u`/<C-r> to undo/redo, and
`?` to search the actions available in the current context.

The walkthrough in docs/getting-started/quick-start.md builds two rooms,
connects them with a door, adds furniture and wall features, then validates
and saves the plan.

Open the plan later with: >vim
  :RoomPlanOpen ~/plans/flat.roomplan.json
<

Inside a .norg buffer, |:RoomPlanInit| initializes a marked embedded block and
|:RoomPlanOpen| discovers it. A standalone path may always be supplied
explicitly.

==============================================================================
4. COMMANDS                                               *roomplan-commands*

                                                        *:RoomPlan*
:RoomPlan                 Open the complete contextual action window.

                                                        *:RoomPlanOpen*
:RoomPlanOpen [source]    Open a standalone JSON or a plan in a Norg source.

                                                        *:RoomPlanInit*
:RoomPlanInit [source]    Initialize a new source; never overwrites content.

                                                        *:RoomPlanHide*
:RoomPlanHide             Hide the workspace but retain the session.

                                                        *:RoomPlanClose*
:RoomPlanClose[!]         Close the session. ! permits discarding changes.

                                                        *:RoomPlanAddRoom*
:RoomPlanAddRoom          Open the structured room form.

                                                        *:RoomPlanAlign*
:RoomPlanAlign            Open room alignment for the current selection.

                                                        *:RoomPlanAddDoor*
:RoomPlanAddDoor          Open the structured door form.

                                                        *:RoomPlanAddWindow*
:RoomPlanAddWindow        Open the structured window form.

                                                        *:RoomPlanAddOutlet*
:RoomPlanAddOutlet        Open the structured outlet form.

                                                        *:RoomPlanAddFurniture*
:RoomPlanAddFurniture     Open the structured furniture form.

                                                        *:RoomPlanEdit*
:RoomPlanEdit             Edit the selected object or plan.

                                                        *:RoomPlanDuplicate*
:RoomPlanDuplicate        Duplicate the current selection.

                                                        *:RoomPlanDelete*
:RoomPlanDelete           Delete the current selection after confirmation.

                                                        *:RoomPlanToggleNavigator*
:RoomPlanToggleNavigator  Focus or toggle the Navigator pane.

                                                        *:RoomPlanToggleDetails*
:RoomPlanToggleDetails    Focus or toggle the Details pane.

                                                        *:RoomPlanValidate*
:RoomPlanValidate         Validate and open Issues.

                                                        *:RoomPlanNextIssue*
:RoomPlanNextIssue        Select the next diagnostic.

                                                        *:RoomPlanPrevIssue*
:RoomPlanPrevIssue        Select the previous diagnostic.

                                                        *:RoomPlanUndo*
:RoomPlanUndo             Undo one model action.

                                                        *:RoomPlanRedo*
:RoomPlanRedo             Redo one model action.

                                                        *:RoomPlanFit*
:RoomPlanFit              Fit all plan geometry in the canvas.

                                                        *:RoomPlanCanvasDetail*
:RoomPlanCanvasDetail [high\|middle\|none\|cycle]
                          Set an exact canvas detail level. Use cycle, or omit
                          the argument, to advance to the next level.

                                                        *:RoomPlanAspect*
:RoomPlanAspect [ratio]   Set the process-wide terminal cell aspect ratio.
                          Without a ratio, prompt for one. Every open canvas
                          is refitted; model data and history are unchanged.

                                                        *:RoomPlanRotateView*
:RoomPlanRotateView [dir] Rotate the view clockwise by default. dir may be
                          clockwise, counterclockwise, or reset.

                                                        *:RoomPlanSunStudy*
:RoomPlanSunStudy       Open site setup or the transient sunlight study.

                                                        *:RoomPlanMinimap*
:RoomPlanMinimap        Toggle the colored whole-plan minimap. Its outlined
                        rectangle is the current canvas field of view.

                                                        *:RoomPlanSave*
:RoomPlanSave[!]          Save. ! permits saving a structurally valid draft
                          that still has layout errors.

                                                        *:RoomPlanSaveAs*
:RoomPlanSaveAs[!] path   Save to another source with destination checks.

                                                        *:RoomPlanReload*
:RoomPlanReload[!]        Reload. ! permits discarding local changes.

                                                        *:RoomPlanResolveConflict*
:RoomPlanResolveConflict  Choose how to resolve a changed source.

                                                        *:RoomPlanMenu*
                                                        *:RoomPlanObjects*
                                                        *:RoomPlanInspect*
:RoomPlanMenu is a convenience alias for :RoomPlan. :RoomPlanObjects and
:RoomPlanInspect focus or toggle Navigator and Details respectively.

==============================================================================
5. WORKSPACE                                             *roomplan-workspace*

The workspace has a canvas, a left Navigator/Issues pane, an optional right
Details pane, and a one-line contextual footer. The Navigator starts visible;
Details starts hidden. Both retain their selection and can be toggled to give
the canvas more room.

The Details title follows NAV, MOVE, PAN, RESIZE, forms, and SUN STUDY. Its
always-expanded Canvas controls section uses the same command registry as the
footer and `?`, including Finish moving, Finish panning, Cancel resize, or
Close study. When Details has focus, press `2` to return to the canvas before
using those controls; Enter/Space/h/l remain local section controls.

The one-line footer adds a concise room/object breadcrumb for a useful
selection. MOVE extends it with direction, distance, and snap target; RESIZE
adds section, edge, step, and snap target. When Details is visible the footer
keeps status and feedback without duplicating its command list. With Details
hidden it again shows the compact contextual actions.

Wide terminals use side panes. Medium layouts retain only the panes that fit.
Compact layouts open bordered drawers. Manually closing a pane keeps it closed
until explicitly toggled again.

Navigator shows rooms with nested doors, windows, outlets, and furniture.
Issues filters diagnostics; Enter and previous/next issue select, centre, and
reveal the owner without changing zoom or rotation. Details presents compact,
collapsible sections whose contents change with the selection. `?` always exposes the full
action set, including disabled actions, their reasons, literal current-mode
controls, and the command that leaves the mode. In Navigator, Space
marks an object; atomic move, duplicate, delete, and clear actions for the
marked set live in `?` and create one history entry.

Structured forms keep their complete draft visible. A form is revision-bound:
if the plan changes elsewhere, the stale form cannot apply over newer data.
Room and furniture forms include a dependency-free color palette. Stored
#RRGGBB colors accent labels and furniture outlines; selection, warning, and
error highlights always take priority. Theme default follows the colorscheme.
Add and Edit furniture show a live RoomPlanPreview footprint silhouette beside
the popup when space permits. Narrow editors keep it in the popup instead.
Invalid input removes stale geometry, and the preview never changes IDs,
history, validation, or saved data.

==============================================================================
6. KEYMAPS                                                 *roomplan-keymaps*

Workspace:

  1 / o          focus or toggle Navigator
  2              focus canvas
  3 / i          focus or toggle Details
  !              focus or toggle Issues
  <Tab>          next pane
  <S-Tab>        previous pane
  <CR>           select a Navigator/Issue row; toggle Details section
  <Space>        mark / unmark an object row for batch actions
  /              filter Navigator or Issues
  h / l          collapse / expand a room or Details section
  ?              complete action window or contextual help
  /              focus the in-popup prompt in the ? action window
  q              return to canvas, close a drawer, or hide workspace
  <Esc>          cancel the current interaction, then return to canvas

Canvas and editing:

  h j k l        move cursor/object, choose a resize edge, or pan
  H J K L        coarse movement/resizing
  <C-h/j/k/l>    exact fine movement/resizing
  <CR>           select object under cursor
  m / p          MOVE / PAN mode
  a              Add menu (r/d/w/o/f choose object type)
  D / W / O / F  add door / window / outlet / furniture
  A              align room
  e              exact edit
  d / y          delete / duplicate selection
  r / R          live-resize room/furniture / rotate furniture
  v / Alt-j / Alt-k
                  validate / next issue / previous issue
  u / <C-r>      undo / redo
  . / , / f / zf zoom in / zoom out / fit
  M              toggle whole-plan minimap / field-of-view rectangle
  t              cycle high / middle / none canvas detail
  zh zj zk zl    pan without entering PAN mode
  Alt-l / Alt-h / g0
                  rotate view clockwise / counterclockwise / plan view-up
  S               open or reopen sunlight study
  gs / g!        toggle snapping / bypass the next snap

While a sun study is visible on the canvas, h/l step through time and j/k move
forward/backward by three months at the same clock time. Space plays the whole
day from sunrise or pauses/resumes, S reopens settings, and Esc closes the
study. At sunset the animation becomes a fixed-band daily-exposure map.

Measurement, furniture wall placement, marked-object batch actions, and named
history browsing are searchable in `?` and have no default canvas mapping.

In NAV, directional movement keeps travelling beyond the visible canvas. Once
the cursor reaches the configured canvas.scrolloff margin, RoomPlan pans the
viewport in that direction while keeping the cursor inside the margin.
  s / gS         save / save as

Forms:

  j k / Tab      next / previous field
  <CR> / e       edit the active field
  h l            cycle a choice
  <Space>        toggle or advance a choice
  <C-s>          apply atomically
  R              reset the form draft
  q / <Esc>      cancel

Set keymaps.enabled=false to install no RoomPlan mappings. Override a semantic
mapping name or its default left-hand side in keymaps.mappings. Use false to
disable one mapping. Displayed hints use the same resolver as installed maps.

==============================================================================
7. CONFIGURATION                                           *roomplan-config*

Call require("roomplan").setup({...}). Unknown options and invalid values are
errors; options are not silently ignored. The important defaults are: >lua
  {
    plan_defaults = {
      metadata = { name = nil, notes = "" },
      settings = {
        grid_mm = 100,
        fine_step_mm = 10,
        normal_step_mm = 100,
        coarse_step_mm = 500,
        default_door_width_mm = 900,
      },
    },
    furniture = { include_builtins = true, definitions = {}, files = {} },
    canvas = {
      open = "tab",
      unicode = "auto",
      mm_per_column = 100,
      cell_aspect = 2.0,
      zoom_factor = 1.25,
      min_mm_per_column = 1,
      max_mm_per_column = 100000,
      fit_margin_cells = 2,
      header_lines = 1,
      scrolloff = 3,
      pan_step_cells = 5,
      pan_coarse_step_cells = 20,
      show_grid = false,
      detail_level = "middle",
      show_compass = true,
    },
    snapping = {
      enabled = true,
      tolerance_cells = 1.5,
      max_distance_mm = 250,
      priority = { "door", "room_edge", "room_center", "furniture", "grid" },
    },
    sun_study = {
      window_defaults = { sill_height_mm = 900, head_height_mm = 2100 },
      playback = { step_minutes = 60, frame_duration_ms = 700 },
    },
    ui = {
      confirm_delete = true,
      notify_level = "info",
      workspace = {
        layout = "auto",
        left_width = 26,
        right_width = 30,
        navigator_visible = true,
        details_visible = false,
        wide_min_columns = 120,
        compact_max_columns = 89,
        compact_min_rows = 22,
        min_canvas_width = 55,
        min_canvas_height = 10,
        footer_height = 1,
        cycle_tabs = true,
        ascii = false,
        border = "rounded",
      },
    },
    autosave = { enabled = false, debounce_ms = 1000, norg = false },
    keymaps = { enabled = true, mappings = {} },
    glyphs = nil,
  }
<

Resource ceilings live under limits. See docs/configuration/settings.md for
the complete list and docs/configuration/keymaps.md for semantic mapping names.

Highlight groups start with RoomPlan and use default definitions, so
colorschemes or user configuration can override them normally. Sunlight bands
derive from warning/error colors; minimap defaults derive from Function,
DiagnosticInfo, IncSearch, and Search. RoomPlan setup and redraw preserve
explicit RoomPlanSunlight*, RoomPlanSun*, and RoomPlanMinimap* overrides. See
docs/display/appearance.md.

==============================================================================
8. WINDOWS AND OUTLETS                              *roomplan-wall-features*

Schema v4 adds windows as wall apertures and outlets as wall or floor points.
Press W or O, run :RoomPlanAddWindow or :RoomPlanAddOutlet, or press `a` then
lowercase `w`/`o`. Select either object and use `e`, `m`, `y`, or `d` to edit,
move, duplicate, or delete it.

Wall features reference an owner room, footprint part, side, and integer-mm
offset. North/south offsets start at the west end; east/west offsets start at
the south end. Internal part seams are not attachable walls. Floor outlets
instead store a strictly interior room-local point and move with their room.

A window also stores width and connects either to outside (JSON null) or to a
room whose opposite wall covers the whole aperture. A valid window cuts its
owner wall; only a verified connection cuts the matching connected wall.
Invalid aperture geometry never cuts a wall. Windows have no swing. An
optional paired sill_height_mm/head_height_mm feeds sunlight; otherwise setup
defaults are used without adding keys to the plan.

An outlet never cuts a wall. Types are power, usb, ethernet, coax, phone, and
other; slots must be 1 through 32. Wall outlets use inward-facing half circles
and must be strictly inside an exterior edge because corners are ambiguous.
Floor outlets use full circles and must be strictly inside their owner room.

The top-down model does not store glazing/opening style or outlet mounting
height. See docs/planning/windows-and-outlets.md and
docs/planning/sun-study.md.

==============================================================================
9. FURNITURE CATALOGUES                                 *roomplan-furniture*

Placed furniture has direct compound shape editing. Select it and press r to
enter RESIZE immediately; R rotates it instead. RESIZE uses Enter/Tab to select
sections, h/j/k/l to choose and move visible edges, a/d to add/remove, s to
apply and save, and Esc to cancel. Its explicit anchor and position stay fixed.
Invalid anchor-removing edits are rejected. When it references a project
template, s opens a RoomPlan popup: update this item only, or update the item
plus the future-placement template as one undo step. Other placed items never
change. Edit footprint in the e popup opens the same controls.

Project templates use the same r shortcut and Edit footprint row. Select a
top-level template row and use either path to edit an isolated local preview.
Saving changes future placements only and restores the plan viewport; existing
furniture keeps explicit geometry.

RoomPlan includes generic built-in templates. Add process-wide defaults inline
or load one or more dependency-free JSON files. Set
furniture.include_builtins=false to make imported and inline definitions the
complete visible catalogue. Built-in IDs remain resolvable for existing plans.
Project-local templates saved inside a plan take precedence over imported
templates with the same custom ID; built-in IDs remain reserved.

Inline example: >lua
  require("roomplan").setup({
    furniture = {
      definitions = {
        {
          id = "custom:standing-desk",
          name = "Standing desk",
          category = "work",
          shape = "rectangle",
          default_size_mm = { 1600, 800, 1100 },
        },
      },
      files = { vim.fn.stdpath("config") .. "/roomplan-furniture.json" },
    },
  })
<

File format: >json
  {
    "version": 1,
    "furniture": [
      {
        "id": "custom:reading-chair",
        "name": "Reading chair",
        "category": "seating",
        "shape": "rectangle",
        "default_size_mm": [850, 900, 1000]
      }
    ]
  }
<

Catalogue setup is atomic: an invalid definition or file leaves the previous
effective catalogue unchanged. Files are limited to 1 MiB.

==============================================================================
10. ASPECT AND VIEW ROTATION                              *roomplan-display*

Terminal cells are usually taller than they are wide. canvas.cell_aspect is
cell height divided by cell width; the default is 2.0. If a square room looks
vertically stretched, use :RoomPlanAspect with a measured ratio. This updates
the current Neovim process and refits every live canvas; no rebuild is needed.

View rotation is also transient. Alt-l and Alt-h rotate by 90 degrees, while
g0 restores plan view/up. Before site setup the compass shows plan up (P plus
an arrow); afterwards it shows geographic north (N plus an arrow). Movement,
panning, and top/right/bottom/left wall labels remain screen-relative. Saved
room, door, window, outlet, and furniture coordinates never rotate.

Space in the sun-study popup starts at sunrise and dismisses the popup so the
plan remains visible. On the canvas, `h`/`l` step through time, `j`/`k` compare
dates three months apart, Space plays or pauses, S reopens settings, and Esc
closes the study. It calculates offline and stops at sunset, then shows daily
exposure in fixed <=1h, <=2h, <=4h, <=6h, and >6h bands. Its floor patches use
the colorscheme's warning/error spectrum and remain approximate 2D clear-sky
exposure, not illuminance or thermal simulation.

canvas.detail_level defaults to "middle". high shows labels plus all exterior
wall-run, furniture width/depth, and door/window width dimensions. middle shows
labels plus exterior wall-run dimensions. none renders geometry without
labels or dimensions. Press `t`, use |:RoomPlanCanvasDetail|, or call
set_canvas_detail() to change only the live session presentation.

==============================================================================
11. STORAGE AND SAFETY                                    *roomplan-storage*

Standalone files conventionally end in .roomplan.json. Neorg plans use a
marked ranged tag. RoomPlan preserves unknown extension fields and refuses
ambiguous or unsafe source operations.

One writable session owns a source at a time. Other plans can remain open.
Writes use revision checks and staged/atomic replacement where applicable.
Source changes produce a conflict instead of silently overwriting either side.
Dirty sessions are protected on close, reload, :q, and process exit.
If a protected quit encounters a conflict, RoomPlan keeps the guard modified
and opens the normal resolution choices instead of raising a Lua traceback.

Structural schema failures cannot be saved. Layout-invalid drafts may be
reviewed in Issues, repaired, or explicitly saved with !. That escape hatch is
not a claim that the layout is safe or physically constructible.

The current schema is schema/roomplan-v4.schema.json; v1, v2, and v3 remain readable
through sequential migrations. Geometry uses integer millimetres and exact
doubled-millimetre arithmetic where centred odd-sized objects create half-
millimetre edges. Loading or normalizing an older plan never rewrites its
source, and autosave pauses until the first explicit schema-v4 save.

==============================================================================
12. LUA API                                                   *roomplan-api*

Public entry points: >lua
  local roomplan = require("roomplan")

  roomplan.setup(opts)
  roomplan.open(opts, callback)
  roomplan.init(opts, callback)
  roomplan.save(opts, callback)
  roomplan.save_as(path, opts, callback)
  roomplan.reload(opts, callback)
  roomplan.set_aspect(ratio, opts, callback)
  roomplan.set_canvas_detail(level)
  roomplan.rotate_view(direction)
  roomplan.sun_study()
  roomplan.toggle_minimap()
  roomplan.hide(opts)
  roomplan.close(opts, callback)
  roomplan.validate(opts)
  roomplan.sessions()
<

roomplan.aspect is retained as a compatibility alias for set_aspect; new
configuration should use the explicit canonical name.

Callbacks receive (result, error). With no callback, noninteractive operations
return those values directly. Errors are structured tables with at least code
and message where possible.

For low-level integrations, require("roomplan.api").dispatch(session_id,
action) sends a model action through the normal controller/history boundary;
session(session_id) returns the live session.

==============================================================================
13. HEALTH AND TROUBLESHOOTING                            *roomplan-troubles*

Run: >vim
  :checkhealth roomplan
<

It checks Neovim/runtime compatibility, encoding, display capabilities,
configuration and mappings, optional Neorg, workspace sizing, live sessions,
and source writability.

If require("roomplan") says the module is missing, the package is not on
'runtimepath' at the time configuration executes. Fix the package-manager or
Nix/nvf wiring; do not add a separate Lua package. If :RoomPlan is missing,
the plugin loader was not sourced for the same reason. Restart Neovim after
changing the installed package/generation.

If the canvas looks stretched, run :RoomPlanAspect; this never requires a Nix
or system rebuild. If a plan is not visible, press `f`. If an action is not in
the compact footer, press `?` to open the complete contextual action window,
then `/` to filter its actions.

For detailed diagnosis see docs/reference/troubleshooting.md.

==============================================================================
vim:tw=78:ts=8:noet:ft=help:norl:
