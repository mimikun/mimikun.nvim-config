-- Due date notifications
local due_notifications = {
  -- Enable due date notifications
  enabled = true,

  -- Show notification on Neovim startup
  on_startup = true,

  -- Show notification when opening todos
  on_open = true,
}

return due_notifications
