-- blacklist from auto_install
-- Languages to opt-out from `auto_install`.
---@type string[]
local noauto_install = {
  -- Use built-in Neovim treesitter parsers
  --"c",
  --"lua",
  --"markdown",
  --"markdown_inline",
  --"query",
  --"vim",
  --"vimdoc",
}

return noauto_install
