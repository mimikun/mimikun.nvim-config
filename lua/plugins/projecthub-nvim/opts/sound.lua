-- Sound effects (bundled out-of-the-box, zero external downloads needed)
-- UI Sound Effects (uisfx minimal preset)
local sound = {
  -- Set to false to disable all audio feedback
  enabled = false,

  -- Volume level (0.0 to 1.0)
  volume = 0.5,
}

return sound
